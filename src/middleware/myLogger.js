// const { getUserData } = require("../services/getUserData.service");

// module.exports = async function myLogger(req, res, next) {
//   const user_data = await getUserData(req.headers);
//   req.user_data = user_data ? user_data : {};

//   next();
// };
